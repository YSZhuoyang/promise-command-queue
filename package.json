{
    "name": "promisecommandqueue",
    "version": "0.1.1",
    "description": "A fairly simple command queue that ensures both commands containing sync code and commands containing async code are executed in sequence, through promises chaining.",
    "main": "./dist/commandQueue.js",
    "types": "./dist/commandQueue.d.ts",
    "scripts": {
        "build": "./node_modules/.bin/tsc -p tsconfig.src.json",
        "build:test": "./node_modules/.bin/tsc -p tsconfig.test.json",
        "test:remote": "jest --coverage --coverageReporters=text-lcov | coveralls",
        "test": "jest"
    },
    "author": "Oscar Yu",
    "repository": {
        "type": "git",
        "url": "https://github.com/YSZhuoyang/CommandQueue"
    },
    "license": "MIT",
    "homepage": "https://github.com/YSZhuoyang/CommandQueue/#README",
    "bugs": {
        "url": "https://github.com/YSZhuoyang/CommandQueue/issues"
    },
    "devDependencies": {
        "@types/jest": "^24.0.1",
        "coveralls": "^3.0.2",
        "jest": "^24.1.0",
        "typescript": "^3.3.3"
    }
}
