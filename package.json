{
  "name": "commandqueue",
  "version": "0.0.1",
  "description": "A fairly simple command queue that ensures both sync and async commands are executed in sequence.",
  "main": "./dist/commandQueue.js",
  "scripts": {
    "build-source": "./node_modules/.bin/tsc -p tsconfig.src.json",
    "build-test": "./node_modules/.bin/tsc -p tsconfig.test.json",
    "test": "jest"
  },
  "author": "Oscar Yu",
  "repository": {
    "type": "git",
    "url": "https://github.com/YSZhuoyang/CommandQueue"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/jest": "^23.3.1",
    "jest": "^23.5.0",
    "typescript": "^2.9.2"
  }
}
